# Database (MySQL). Copy to .env and set your values.
DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/DATABASE_NAME"

# Auth: base64 key for password hashing. Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
HASH_SECRET=""

# JWT secret for signing tokens (use a long random string)
JWT_SECRET=""
